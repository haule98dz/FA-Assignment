#include <stdio.h>
#include "Assignment1.h"

/*******************************************************************************
* Code
******************************************************************************/

int main(void)
{
    enum  {india, is - 7};
    double o[10];
    double *a;
    double *b = a+3;
    printf("%d", is);
    uint8_t *data;
    data = NULL;
    if (allocate10Bytes(&data))
    {
        printf("Allocate 10 bytes successfully\n");
        printf("Adress: %p", data);
    }
    else
    {
        printf("Failed to allocate 10 bytes");
    }
    if (data == NULL) printf("d");
	return 0;
}

